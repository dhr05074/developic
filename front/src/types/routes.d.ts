type StepTypes = "idle" | "loading" | "complete";
type StepperStyle = {
    // [component: StepTypes]: StepTypes;
    idle: string;
    loading: string;
    complete: string;
    invisible: string;
};
